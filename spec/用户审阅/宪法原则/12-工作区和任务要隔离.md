# P12 工作区和任务要隔离

## 原则

任务和目录都要隔离，不能所有队友都在同一个工作目录里乱改。

## 为什么

只有隔离，多个任务并行时才不会互相污染。

## 在 Deadmouse 里的含义

- task 管目标
- worktree 管目录
- 两者通过绑定关系协同

## 当前对应

- `src/worktrees/store.ts`
- `src/capabilities/tools/packages/worktrees/`
