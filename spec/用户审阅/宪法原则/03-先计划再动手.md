# P03 先计划再动手

## 原则

复杂任务不要直接开做，先列步骤再动手。

## 为什么

没有计划的 agent 很容易：

- 走一步看一步
- 重复劳动
- 边改边漂移

## 在 Deadmouse 里的含义

- 通过 `todo_write` 写出简短计划
- 只能有一个 `in_progress`
- 写 / 改 / 执行类工具受硬性 gating
- 子智能体不强制，保持轻量

## 当前对应

- `src/agent/runTurn.ts`
- `src/capabilities/tools/packages/tasks/todoWriteTool.ts`
