# 机器层只执行和记录事实

机器层负责执行工具、保存状态、记录事件、暴露结果。

模型负责基于当前目标和证据判断下一步。

Runtime、tool registry、observability、checkpoint、verification 这些结构不能变成第二个脑子。

当前落点：

- `src/agent/turn/`
- `src/tools/`
- `src/extensions/`
- `src/observability/`
- `src/session/`
- `src/runtime-ui/`
