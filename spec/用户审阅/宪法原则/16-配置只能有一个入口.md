# P16 配置只能有一个入口

## 原则

配置必须有统一入口，不能一半在命令行、一半在代码常量、一半在隐式状态。

## 为什么

配置一旦分散：

- 用户搞不清当前行为从哪来
- 维护者很难排查问题

## 在 Deadmouse 里的含义

配置读取要收敛到同一套路径与环境变量机制。

## 当前对应

- `src/config/init.ts`
- `src/config/store.ts`
- `src/config/env.ts`
