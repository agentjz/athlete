# P04 大任务拆给子智能体

## 原则

大任务不要全部塞进一个上下文里，可以拆给子智能体处理。

## 为什么

这样每个子任务都有干净上下文，不会把主对话越搞越脏。

## 在 Deadmouse 里的含义

subagent 是一次性、轻量、上下文隔离的执行单元。

它适合：

- 探索
- 局部规划
- 局部实现

但不适合承担长期团队控制面。

## 当前对应

- `src/subagent/`
- `src/tools/tasks/taskTool.ts`
