# 工具体系规划与收敛

## 文档目的

本文描述 Deadmouse 当前的工具面、工具注册边界、文件工具职责分工和测试保护方式。

## 模块目标与当前状态

当前工具专项已经完成以下收口：

- 工具名保持稳定
- 不增加平行工具族
- `find_files` 作为正式 builtin tool 接入
- `read_file / search_files / list_files / edit_file` 已按当前职责增强
- `write_file`、`run_shell` 等高风险工具继续受治理与 guard 约束

## 正式边界

工具体系当前的正式边界是：

- `src/tools/builtinCatalog.ts`
- `src/tools/registry.ts`
- `src/tools/runtimeRegistry.ts`
- `src/tools/governance.ts`
- `src/tools/toolArgumentContract.ts`
- `src/host/toolRegistry.ts`

文件工具的主要实现位于：

- `src/tools/files/listFilesTool.ts`
- `src/tools/files/findFilesTool.ts`
- `src/tools/files/readFileTool.ts`
- `src/tools/files/searchFilesTool.ts`
- `src/tools/files/editFileTool.ts`
- `src/tools/files/applyPatchTool.ts`
- `src/tools/files/writeFileTool.ts`
- `src/tools/shell/runShellTool.ts`
- `src/tools/shell/outputCapture.ts`
- `src/tools/background/backgroundTerminateTool.ts`

## 当前工具面

### 默认主工具

- `read_file`
- `write_file`
- `edit_file`
- `apply_patch`
- `run_shell`

### 只读探索工具

- `list_files`
- `find_files`
- `search_files`

### 状态与任务工具

- `todo_write`
- `task*`
- `worktree*`
- `background_check`

### 高风险执行工具

- `run_shell`
- `background_run`
- `background_terminate`
- destructive 写工具

## 主路径

### 1. 注册与暴露主路径

当前 builtin tool 统一在 `src/tools/builtinCatalog.ts` 注册，经 `registry -> governance -> runtimeRegistry` 暴露；宿主 extra tool 先进入 `src/host/toolRegistry.ts`，再进入同一条主路径。
参数 contract 统一在 `toolArgumentContract` 执行：schema 不匹配时在 prepare 阶段 fail closed，不再进入 execute。

### 2. 文件探索主路径

当前推荐顺序是：

- 看目录：`list_files`
- 按路径找目标：`find_files`
- 按内容搜索：`search_files`
- 读取正文：`read_file`

系统提示和测试已经明确把文件探索从 shell-first 拉回到正式工具面。

### 3. 文件修改主路径

当前职责分工为：

- 创建文件：`write_file`
- 小范围、多点位精修：`edit_file`
- patch 语义修改：`apply_patch`

`edit_file` 当前要求 `expected_identity` 与 `anchor`；`write_file` 覆盖已有文件会在 prepare 阶段被阻断。
三个写工具在写后统一返回 `changedPaths`、diff、diagnostics 和 session diff。

### 4. shell 执行主路径

`run_shell` 当前维持单一工具名，但底层执行结果已结构化为：

- 执行状态（`completed/failed/timed_out/stalled/aborted`）
- 退出码、耗时、重试次数
- 输出截断标记
- 长输出落盘路径与预览

长输出当前在执行过程中就受正式预览上界控制，并在超出阈值后持续落盘，不再依赖命令结束后的事后截断。

`run_shell / background_run / background_check / background_terminate` 共享 `deadmouse.exec.v1` process contract，统一表达 start/read/terminate/exited/closed 的等价语义。

## 模块职责与非职责

### 工具体系负责

- 暴露正式工具面
- 管理 registry 与 governance
- 保持文件探索与修改工具的分工
- 让宿主扩展和 MCP 能力进入统一主路径

### 工具体系不负责

- 再次定义调度规则
- 让宿主各自维护独立工具池
- 通过改名制造第二套能力入口

## 失败路径与异常路径

当前明确处理：

- 同名工具注册在 registry 边界显式失败
- 缺 governance 的扩展 fail closed
- `write_file` 覆盖已有文件在 prepare 阶段被阻断
- `run_shell` 直接读取文件内容在 prepare 阶段被阻断
- schema 不匹配的工具参数在 prepare 阶段 fail closed，execute 不会被触发
- `run_shell` 长输出会在执行过程中自动控上界并写入状态目录，避免上下文爆炸和单次命令输出失控占用内存
- 不再为 `read / write / edit / bash / grep / ls` 之类平行入口保留位置

## 测试与验证

当前主要由以下测试保护：

- `tests/tools-convergence.test.ts`
- `tests/tool-governance.test.ts`
- `tests/extension-platform.test.ts`
- `tests/host-runtime-boundary.test.ts`
- `tests/structure-slimming.test.ts`

## 当前落地决定

当前工具体系的正式决定如下：

- `find_files` 是唯一路径模式查找工具。
- `edit_file` 是结构化精修工具，`apply_patch` 继续保留 patch 语义。
- `read_file` 负责 identity、anchors 和 continuation 信息。
- `search_files` 负责结构化内容搜索，不承担完整文件读取。
- `run_shell` 继续是单一入口，但执行 runtime 与输出收口已正式化。
- `background_terminate` 成为正式 terminate surface，并沿统一 execution closeout 收口。
- 工具体系继续沿 registry、governance 和 host 注入边界增长，不靠平行命名扩张。

