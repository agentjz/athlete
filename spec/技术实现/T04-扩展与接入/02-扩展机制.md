# 扩展机制

## 文档目的

本文说明 Deadmouse 当前的正式扩展口，以及 tools、skills、MCP 和 host extra tools 如何进入统一主路径。

## 当前正式扩展口

1. tools
2. skills
3. MCP
4. host runtime callbacks / extra tools

## 正式边界

### 1. tools

主要代码边界：

- `src/tools/builtinCatalog.ts`
- `src/tools/registry.ts`
- `src/tools/runtimeRegistry.ts`
- `src/tools/governance.ts`

### 2. skills

主要代码边界：

- `src/skills/discovery.ts`
- `src/skills/schema.ts`
- `src/skills/matching.ts`
- `src/skills/state.ts`
- `src/tools/skills/loadSkillTool.ts`

### 3. MCP

主要代码边界：

- `src/mcp/clientManager.ts`
- `src/mcp/toolAdapter.ts`
- `src/mcp/registryIntegration.ts`

### 4. host extra tools

主要代码边界：

- `src/host/toolRegistry.ts`
- `src/telegram/sendFileTool.ts`

## 文档链路示例

当前文档读取链路是扩展机制落地最完整的一组例子：

- builtin tools 暴露 `mineru_pdf_read`、`mineru_image_read`、`mineru_doc_read`、`mineru_ppt_read`
- skills 侧分别提供 `mineru-pdf-reading`、`mineru-image-reading`、`mineru-doc-reading`、`mineru-ppt-reading`
- runtime 在同一注册与治理链路里决定这些工具何时可见、何时优先于泛化的 `read_file`

这意味着 PDF、图片文档、Word 和 PPT 的读取，不是靠 prompt 记住一张散装路由表，而是靠正式扩展口把能力长进系统。

## 主路径

### builtin tool

`builtinCatalog -> registry -> governance -> runtime exposure`

### skill

`discovery / matching -> load_skill -> 当前 turn`

skill 只组织 workflow，不直接绕过工具边界长出动作入口。

### MCP

`MCP discovery -> toolAdapter -> ToolRegistrySource(kind=mcp) -> runtime registry -> governance -> exposure`

### host extra tool

`host tool -> ToolRegistrySource(kind=host) -> runtime registry -> governance -> exposure`

## 模块职责与非职责

### 扩展机制负责

- 给 builtin、skill、MCP 和 host extra tool 提供统一入口
- 给扩展源打上来源和治理语义
- 在 registry 边界处理冲突与 fail-closed

### 扩展机制不负责

- 让 skill 直接创建平行动作入口
- 让 MCP 或宿主绕开 shared registry
- 用 prompt 文案代替正式扩展边界

## 失败路径与异常路径

当前明确处理：

- 重名工具注册在边界显式失败
- 缺 governance 的扩展 fail closed
- host extra tool 会被标记为 `host`，不能伪装成 builtin
- MCP 每个 server 先形成正式 source，再进入共享注册链路

## 测试与验证

当前主要由以下测试保护：

- `tests/extension-platform.test.ts`
- `tests/mcp-skeleton.test.ts`
- `tests/skills-runtime.test.ts`
- `tests/skills-discovery.test.ts`
- `tests/host-runtime-boundary.test.ts`

## 当前落地决定

当前扩展机制已经固定为“工具、技能、MCP、宿主注入四条正式扩展口”。增加扩展必须沿这些边界接入，不再回到扁平拼接或 prompt 注入式扩展。

