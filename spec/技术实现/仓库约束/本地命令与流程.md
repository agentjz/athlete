# 本地命令与流程

## 当前核心命令

- `npm.cmd run check`
- `npm.cmd test`
- `npm.cmd run build`
- `node dist\\cli.js`

## 当前推荐流程

1. 先更新文档
2. 先补失败测试
3. 再改实现
4. 最后跑完整验证

## 当前要求

- Windows 环境优先使用 `npm.cmd`
- 用户可见入口优先验证 CLI、Telegram、微信主路径
