# 交互终端

## 当前定位

交互终端是一个宿主外壳，不是核心真相源。

## 当前边界

- `src/ui/interactive.ts`
- `src/interaction/`
- `src/shell/cli/`

## 当前职责

- 输入输出
- 本地命令
- turn 展示
- interrupt
- quit / exit guard
- runtime summary 展示

## 当前约束

1. 交互终端不能直接依赖 raw turn。
2. 它必须通过 `src/host/` 进入核心。
3. 展示层不能偷偷保存平行 runtime 状态。
