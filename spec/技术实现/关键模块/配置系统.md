# 配置系统

## 当前入口

- `src/config/store.ts`

## 当前职责

- 读取全局 `config.json`
- 读取项目 `.athlete/.env`
- 合并环境变量覆盖
- 产出统一 runtime config

## 当前必须守住的事

1. 配置入口只能有一个。
2. 全局配置要有 schema version。
3. 旧配置只允许一次性升级，不做长期脏兼容。
4. Telegram、微信、MCP、provider 都必须从这里出 runtime config。
